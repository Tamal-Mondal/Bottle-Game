<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user_administrator.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@drawable/bottle"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity" />
        <activity android:name=".success" />
        <activity android:name=".database" />
        <activity android:name=".lifecycle" />
        <activity android:name=".login" />
        <activity android:name=".Register" />
        <activity android:name=".GameFirst" />
        <activity android:name=".GameNew" />
        <activity android:name=".Blink" />

        <service
            android:name=".MyService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".Sensor" />
        <activity android:name=".mysensor"></activity>
    </application>

</manifest>